<!DOCTYPE html>
<html>
<head>
  <title>Constituent Connect</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
 <header class="navbar navbar-default" id='header'>
  <div class="container-fluid">
    <div class="navbar-header">
      <% if current_user %>
         <a id="logoHeader"class="navbar-brand" href="/dashboard">
            Office of <%= current_user.office.name %>        
          </a>
      <% else %>
        <a id="logoHeader"class="navbar-brand" href="/dashboard">
          Constituent Connect
        </a>
      <% end %>
    </div>
    <div id="logo" class="nav navbar-nav navbard-center">
      <%= image_tag("congress_logo.png") %>
    </div>   
      <ul id="navLinks" class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li>Logged in as <%= current_user.email %></li>
          <li><%= link_to "Log Out",  destroy_user_session_path, method: :delete %></li>
        <% end %>
      </ul>
  </div>
</header>


<%= yield %>

</body>
</html>
